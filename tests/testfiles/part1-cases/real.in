procedure main() returns integer {
    tuple(real, real) t = (17.6, 8.9);
    
    var t2 = as<tuple(real, real)>(t);
    t == t2 -> std_output;
    t2.1 -> std_output;
    t2.2 -> std_output;
    
    t2 = as<tuple(real, real)>((true, false));
    t2.1 -> std_output;
    t2.2 -> std_output;
    
    t2 = as<tuple(real, real)>(('a', '6'));
    t2.1 -> std_output;
    t2.2 -> std_output;
    
    t2 = as<tuple(real, real)>((70, 109));
    t2.1 -> std_output;
    t2.2 -> std_output;
    
	return 0;
}
//CHECK_FILE:./real.out
