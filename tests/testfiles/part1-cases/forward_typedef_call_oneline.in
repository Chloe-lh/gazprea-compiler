typealias integer joe;
typealias real zig;

function x(integer x) returns real;

function y() returns real = x(9);

procedure main() returns integer {
    var zig k = y();
    var real n = x(3);
    k -> std_output;
    n -> std_output;
    '\n' -> std_output;
    return 0;
}

function x(joe x) returns zig = 3.4;
//CHECK_FILE:./forward_typedef_call_oneline.out
