function foo() returns character {
    character c; 
    return c;
}

function bar() returns character {
    character c; 
    return c;
}

procedure main() returns integer {
    const i = foo();
    const j = bar();
    return 0;
}
//CHECK_FILE:./func_char.out
